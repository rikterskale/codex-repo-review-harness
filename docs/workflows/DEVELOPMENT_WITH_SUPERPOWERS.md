# Development with Superpowers

Use Superpowers only as a process layer. First read the target repository's
`AGENTS.md`, confirm a clean feature branch or worktree, classify the change,
and obtain design and plan approval. Implement through the parent agent, run the
smallest relevant test then the broader suite, and stop before merge. Repository
policy, CI, branch protection, and human approval remain authoritative.
